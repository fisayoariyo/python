# Operator Precedence

""" Parentheses has the highest precedence, which means expressions inside the parentheses must first be evaluated
"""
print((6 + 3) - (6 + 3))

""" Multiplication has the higher precedence than addition +, and therefore multiplications are evaluated before additions:
"""
print(100 + 5 * 3)

