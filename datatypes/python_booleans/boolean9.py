# Python has many built-in functions that return a boolean value, like the isinstance() function.
# Which is used for determining if an object is of certain data type:

#Check if an object is integer or not


x = 200
print(isinstance(x, int))
print(isinstance(x, float))

